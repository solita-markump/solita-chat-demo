services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chatdb
    ports:
      - "5432:5432"
    volumes:
      - solita-chat-postgres-data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  solita-chat-postgres-data:
